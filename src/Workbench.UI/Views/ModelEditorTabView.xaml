<UserControl x:Class="Workbench.Views.ModelEditorTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="BundleSelectorRow" Height="25"/>
            <RowDefinition x:Name="BundleEditorRow" Height="*"/>
        </Grid.RowDefinitions>

        <!-- Bundle selector -->
        <ComboBox x:Name="BundleNames"/>

        <!-- Bundle editor -->
        <ContentControl x:Name="ActiveItem" Grid.Row="1"/>

    </Grid>

</UserControl>