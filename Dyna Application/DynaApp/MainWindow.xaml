<Window x:Class="DynaApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dyna"
        Height="350" Width="525"
        xmlns:local="clr-namespace:DynaApp">

    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="FileCloseCommand" />
    </Window.CommandBindings>

    <DockPanel LastChildFill="True">

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Resolve..." Name="menuFileResolve">
                    <MenuItem.Icon>
                        <Image Source="Images/Transparent.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Exit" Command="ApplicationCommands.Close">
                    <MenuItem.Icon>
                        <Image Source="Images/Transparent.png" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <local:ModelCanvas x:Name="workspaceCanvas" Background="White"/>

    </DockPanel>
</Window>
